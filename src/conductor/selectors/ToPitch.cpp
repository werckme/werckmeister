#include "ToPitch.h"

namespace sheet
{
    namespace conductor
    {
        bool ToPitch::isMatch(const ConductionSelector::Arguments& arguments, const EventWithMetaInfo& evm) const 
        {
           return false;
        }
    }
}

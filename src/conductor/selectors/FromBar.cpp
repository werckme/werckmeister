#include "FromBar.h"

namespace sheet
{
    namespace conductor
    {
        bool FromBar::isMatch(const ConductionSelector::Arguments& arguments, const EventWithMetaInfo& evm) const 
        {
            return false;
        }
    }
}

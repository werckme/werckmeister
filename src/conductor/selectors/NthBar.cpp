#include "NthBar.h"

namespace sheet
{
    namespace conductor
    {
        bool NthBar::isMatch(const ConductionSelector::Arguments& arguments, const EventWithMetaInfo& evm) const 
        {
            return false;
        }
    }
}

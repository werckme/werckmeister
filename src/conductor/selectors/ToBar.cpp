#include "ToBar.h"

namespace sheet
{
    namespace conductor
    {
        bool ToBar::isMatch(const ConductionSelector::Arguments& arguments, const EventWithMetaInfo& evm) const 
        {
            return false;
        }
    }
}

#include "SetTemplate.h"
#include <compiler/context/AContext.h>

namespace sheet {
    namespace compiler {
        void SetTemplate::execute(AContext* context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}
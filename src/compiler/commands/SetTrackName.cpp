#include "SetTrackName.h"
#include <compiler/context/IContext.h>

namespace sheet {
    namespace compiler {
        void SetTrackName::execute(IContextPtr  context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}
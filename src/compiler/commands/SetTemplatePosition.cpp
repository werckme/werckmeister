#include "SetTemplatePosition.h"
#include <compiler/context/IContext.h>

namespace sheet {
    namespace compiler {
        void SetTemplatePosition::execute(IContext* context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}
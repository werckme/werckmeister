#include "SetTemplatePosition.h"
#include <compiler/context/AContext.h>

namespace sheet {
    namespace compiler {
        void SetTemplatePosition::execute(AContext* context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}
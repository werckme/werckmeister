#include "SetMidiChannel.h"
#include <compiler/context/AContext.h>

namespace sheet {
    namespace compiler {
        void SetMidiChannel::execute(AContext* context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}
#include "SetTrackType.h"
#include <compiler/context/IContext.h>

namespace sheet {
    namespace compiler {
        void SetTrackType::execute(IContextPtr  context)
        {
            // auto value         = parameters[argumentNames.XYZ].value<int>();
           
        }
    }
}